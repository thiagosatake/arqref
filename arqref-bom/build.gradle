description = "Architecture Reference (Bill of Materials)"

apply plugin: "java-platform"

dependencies {
	constraints {
		parent.moduleProjects.sort {"$it.name"}.each{
			api it
		}
	}	
}

publishing {
	publications {
		maven(MavenPublication) {
	    	from components.javaPlatform	    	
	    }
	}
}