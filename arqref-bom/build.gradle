apply plugin: "java-platform"

description = "ArqRef - Bill of Materials"

dependencies {
	constraints {
		parent.moduleProjects.sort {"$it.name"}.each{
			api it
		}
	}	
}

publishing {
	publications {
		maven(MavenPublication) {
	    	from components.javaPlatform	    	
	    }
	}
}